import Nav from "@/components/nav";
import React from "react";

interface Props {}

const Agents = (props: Props) => {
  return (
    <>
      <Nav />
      <div>Agents</div>
    </>
  );
};

export default Agents;
